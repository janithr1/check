name: Node Continuous Integration

on:
  pull_request:
    branches: [ main ]
env:
  AWS_CHECK: ${{ secrets.AWS_CHECK}}


jobs:
  test_pull_request:
    runs-on: ubuntu-latest
    steps:
      - name: check variable
      - run: | 
          echo "${{secrets.AWS_CHECK}}"
